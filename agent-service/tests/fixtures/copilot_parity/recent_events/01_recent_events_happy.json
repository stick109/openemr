{
  "intent_id": "recent_events",
  "scenario": "happy path",
  "input": {
    "user_goal": "Show me recent events.",
    "run_context_summary": {
      "patient_id_present": true,
      "encounter_id_present": false,
      "lookback_days": 180,
      "max_rows": 30,
      "allowed_tools": ["get_recent_events", "get_source_detail"]
    },
    "evidence_packet_ref": "Two recent encounter rows from form_encounter within the 180-day window plus one document row, source_ids encounter:form_encounter:701 and document:documents:42"
  },
  "expected": {
    "tool_sequence": ["get_recent_events"],
    "citation_ids_required": true,
    "answer_blocks_min": 1,
    "verification_status": "passed",
    "missing_or_uncertain": [],
    "refusal_reason": null
  },
  "notes": "AgentEvidenceToolset::fetchRecentEvents enforces caps {max_records=30, max_documents=5, lookback_days=180}. Each record yields one claim with its source_id."
}
