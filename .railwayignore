# Railway builds need application source, but not local secrets or bulky
# development artifacts. Keep .env out of the image; deploy-railway.ps1 syncs
# those values into Railway service variables instead.
.env
.env.*
!.env.example

.git
.github
.obsidian
.claude
.idea
.vscode
.vscode-oss
nbproject

node_modules
vendor
ccdaservice/node_modules
ccdaservice/packages/oe-cqm-service/node_modules

logs
tmp-phpstan
.phpstan
.phpunit.result.cache
coverage
coverage/js-unit

Documentation
diagrams
tests
*.pdf

# Sibling Railway services have their own upload roots (deployed via
# `railway up <path> --path-as-root`). Keep their sources out of the
# openemr-web build context.
dashboard-dotnet

# These are generated by dependency/build tooling in the base image. Preserve
# OpenEMR's checked-in modified assets.
public/assets/*
!public/assets/modified
!public/assets/modified/**
public/themes

# Railway mounts this path as a persistent volume in production.
sites
