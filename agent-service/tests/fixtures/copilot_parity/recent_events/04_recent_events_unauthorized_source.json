{
  "intent_id": "recent_events",
  "scenario": "unauthorized source",
  "input": {
    "user_goal": "Show me recent events.",
    "run_context_summary": {
      "patient_id_present": true,
      "encounter_id_present": false,
      "lookback_days": 180,
      "max_rows": 30,
      "allowed_tools": ["get_recent_events", "get_source_detail"]
    },
    "evidence_packet_ref": "LLM cites encounter:form_encounter:701 whose patient_id is 999 instead of the run-context patient 123"
  },
  "expected": {
    "tool_sequence": ["get_recent_events"],
    "citation_ids_required": true,
    "answer_blocks_min": 1,
    "verification_status": "refused",
    "missing_or_uncertain": [],
    "refusal_reason": "claim cites a source outside the current patient context"
  },
  "notes": "AgentAnswerVerifier rejects when source.patient_id !== access token patient pid. Orchestrator falls back to deterministic, then to system_refusal if necessary."
}
